/**
 * Reading the proxy's {@code config.toml} into the typed records {@code :core}
 * defines. Node-local facts only; network policy lives in
 * {@code network_setting} (ADR 0007).
 */
@NullMarked
package nl.gzmn.playerworlds.proxy.config;

import org.jspecify.annotations.NullMarked;
